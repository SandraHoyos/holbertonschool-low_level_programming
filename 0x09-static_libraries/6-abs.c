#include "holberton.h"
/**
 * _abs - function the absolute value.
 * @numero: value absolute
 *
 * Return: n
 */
int _abs(int numero)
{
if (numero < 0)
{
numero *= -1;
return (numero);
}
else
{
return (numero);
}
}
